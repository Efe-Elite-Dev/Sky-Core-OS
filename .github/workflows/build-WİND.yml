name: Vortex Kernel Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: |
          gcc -c kernel.c -o kernel.o -ffreestanding -m32 -O2
          # Linker ve ISO komutlarını buraya ekle
